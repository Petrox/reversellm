# reversellm
Content aware (OpenAI API specific) reverse proxy for llama/vllm/other inference servers for agentic usecase scenarios that implements sticky sessions for subagents to optimize cache hits between multiple backend inference servers
